#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        int X;
        cin >> X;
        cout << (X <= 7 ? "Yes" : "No") << endl;
    }
    return 0;
}

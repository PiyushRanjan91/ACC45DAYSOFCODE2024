#include <iostream>
using namespace std;

int main() {
    int T, X, A, B;
    cin >> T;
    while (T--) {
        cin >> X >> A >> B;
        int total_score = A * 1 + B * 2;
        if (total_score >= X) cout << "Qualify" << endl;
        else cout << "NotQualify" << endl;
    }
    return 0;
}

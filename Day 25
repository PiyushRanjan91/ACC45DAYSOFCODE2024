#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int T, a, b, c, d;
    cin >> T;
    while (T--) {
        cin >> a >> b >> c >> d;
        cout << max({a + c, a + d, b + c, b + d}) << endl;
    }
    return 0;
}
